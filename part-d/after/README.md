# Part D — After (Live Webflow Pages)

Use these live pages as the primary "after" reference (challenge focus first):

- Empresas: `https://clara-seo-challenge.webflow.io/empresas`
- Corporate Card: `https://clara-seo-challenge.webflow.io/corporate-card`

Notes:

- The challenge prioritizes the `/empresas` flow, so that page is the primary review entry point.
- Interlinking between `/empresas` and `/corporate-card` is intentional and supports the user journey.
- Local `.html` files are archival reference artifacts from early capture/diff work (including wget-based snapshots) and are not the source of truth for final review.
