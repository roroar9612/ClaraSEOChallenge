# This file tells search engine bots what they can and cannot crawl.
# Since our application is mostly behind authentication, we restrict indexing for almost all the pages
#
# Custom Domains & Trust Center:
# - When a customer configures a custom domain, it serves the Trust Center at the root (`/`).
# - Trust Center subpages (`/compliance, /resources`) are dynamic and change per user configuration .
# - To prevent indexing of these subpages, we disallow everything except `/`.

User-agent: *
Allow: /$         # Allow only the root domain (homepage or Trust Center root)
Allow: /favicon.ico # Allow favicon path
# Allow TrustCenter Depth pages
Allow: /compliance
Allow: /controls
Allow: /resources
Allow: /updates
Allow: /trusted-by
Allow: /faq
Allow: /subprocessors
Allow: /your-data
Allow: /testimonials
Allow: /unsubscribe-from-trust-center-updates
 # Block everything else, including dynamically generated subpages
Disallow: /
